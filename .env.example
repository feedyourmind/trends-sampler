# Postgres connection (read-only usage). Supabase transaction pooler URL.
DATABASE_URL=

# Single-user login.
AUTH_USER=
AUTH_PASS=

# Random secret; the value the session cookie is set to. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_TOKEN=
